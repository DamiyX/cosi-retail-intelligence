{
  "name": "@retail/domain",
  "version": "0.0.1",
  "private": true,
  "description": "Pure commerce domain primitives: client IDs, exact money, package conversion.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "files": [
    "src"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint . --max-warnings=0",
    "test": "jest --runInBand"
  }
}
